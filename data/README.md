# Demo Data

This directory contains some demo data.

Use client application like POSTMAN to send this data request to backend endpoint /properties/upload

There are data from Camden (London) and Canterbury (Kent). Unzip the file and upload the gml file to given endpoint mentioned above.
